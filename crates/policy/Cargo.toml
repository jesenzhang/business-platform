[package]
name = "policy"
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[package.metadata.architecture]
bounded_context = "policy"
layer = "domain-and-application"
data_owner = "policy"

[features]
default = []
# In-memory fake ports used by application tests and API-level tests.
testing = []

[dependencies]
chrono = { workspace = true }
uuid = { workspace = true }
async-trait = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
tokio = { workspace = true }

[lints]
workspace = true
